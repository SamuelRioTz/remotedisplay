diff --git a/engine/rustdesk/libs/hbb_common/src/config.rs b/engine/rustdesk/libs/hbb_common/src/config.rs
index 2607fa2..dbcaa20 100644
--- a/engine/rustdesk/libs/hbb_common/src/config.rs
+++ b/engine/rustdesk/libs/hbb_common/src/config.rs
@@ -933,6 +933,18 @@ impl Config {
         rendezvous_server
     }
 
+    /// remotedisplay: modo serverless/LAN = todos los rendezvous configurados son loopback.
+    pub fn is_serverless_lan() -> bool {
+        let servers = Self::get_rendezvous_servers();
+        if servers.is_empty() {
+            return true;
+        }
+        servers.iter().all(|h| {
+            let host = h.split(':').next().unwrap_or(h.as_str());
+            host == "127.0.0.1" || host == "localhost" || host == "::1"
+        })
+    }
+
     pub fn get_rendezvous_servers() -> Vec<String> {
         let s = EXE_RENDEZVOUS_SERVER.read().unwrap().clone();
         if !s.is_empty() {
diff --git a/engine/rustdesk/libs/scrap/src/common/codec.rs b/engine/rustdesk/libs/scrap/src/common/codec.rs
index 9b072e1..383612b 100644
--- a/engine/rustdesk/libs/scrap/src/common/codec.rs
+++ b/engine/rustdesk/libs/scrap/src/common/codec.rs
@@ -270,7 +270,11 @@ impl Encoder {
 
         // auto: h265 > h264 > av1/vp9/vp8
         let av1_test = Config::get_option(hbb_common::config::keys::OPTION_AV1_TEST) != "N";
-        let mut auto_codec = if av1_useable && av1_test {
+        // remotedisplay: en modo serverless/LAN el ancho de banda sobra y AV1 solo cuesta
+        // CPU (su decode a 1080p/4K cuelga clientes modestos → "Connecting…" eterno):
+        // con preferencia Auto usar VP9; AV1 solo si el cliente lo pide explícito.
+        let lan_mode = Config::is_serverless_lan();
+        let mut auto_codec = if av1_useable && av1_test && !lan_mode {
             CodecFormat::AV1
         } else {
             CodecFormat::VP9
diff --git a/engine/rustdesk/src/rendezvous_mediator.rs b/engine/rustdesk/src/rendezvous_mediator.rs
index 0fa8ff0..4f9d2ac 100644
--- a/engine/rustdesk/src/rendezvous_mediator.rs
+++ b/engine/rustdesk/src/rendezvous_mediator.rs
@@ -852,14 +852,7 @@ impl RendezvousMediator {
 // Serverless/LAN-only marker: this fork points the rendezvous server at loopback
 // instead of a real server. Detect that so we can skip all rendezvous traffic.
 pub fn is_serverless_lan() -> bool {
-    let servers = Config::get_rendezvous_servers();
-    if servers.is_empty() {
-        return true;
-    }
-    servers.iter().all(|h| {
-        let host = h.split(':').next().unwrap_or(h.as_str());
-        host == "127.0.0.1" || host == "localhost" || host == "::1"
-    })
+    Config::is_serverless_lan()
 }
 
 fn get_direct_port() -> i32 {
